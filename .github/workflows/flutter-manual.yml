name: Flutter Manually Build

on:
  workflow_dispatch:
  push:
    branches:
      - master
jobs:
  run-flutter-manual-build:
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: "manual-${{ github.ref_name }}"
